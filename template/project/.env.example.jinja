# Copy to .env and fill in. .env is ignored by git; keep secrets out of tracked files.
LOG_LEVEL=INFO
{%- if use_docker %}
# Local development database. Compose reads POSTGRES_PASSWORD; the default is "app".
POSTGRES_PASSWORD=
DATABASE_URL=postgresql://app:${POSTGRES_PASSWORD}@localhost:5432/app
{%- endif %}
