---
description: FastAPI and SQLModel skills for api and db
paths:
  - "{{ package_name }}/api/**/*.py"
  - "{{ package_name }}/db/**/*.py"
---

When a change depends on current FastAPI or SQLModel behavior, read `/fastapi` and `/sqlmodel` under `~/.claude/skills/`. Those match the versions this repo pins, which move faster than model priors.
